import { Text } from "@mantine/core";
import { PageTemplate } from "../components/PageTemplate";

export function PessoasPage() {
  return (
    <PageTemplate title="Pessoas" subtitle="Cadastro e gerenciamento de pessoas">
      <Text c="dimmed">Em breve: listagem + criação + edição + exclusão.</Text>
    </PageTemplate>
  );
}
