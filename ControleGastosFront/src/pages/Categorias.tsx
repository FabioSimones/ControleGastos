import { Text } from "@mantine/core";
import { PageTemplate } from "../components/PageTemplate";

export function CategoriasPage() {
  return (
    <PageTemplate title="Categorias" subtitle="Cadastro e listagem de categorias">
      <Text c="dimmed">Em breve: listagem + criação.</Text>
    </PageTemplate>
  );
}
